#include<stdio.h>
void main()
{ 
	int ch;
 	printf("enter a value between 1 to 2:"); 
	scanf("%d", &ch); 
	switch (ch)/*"case ma only int or char no use kari shakiye chiye."*/
	{
	 case 1: printf("1"); break; 
	 case 2: printf("2"); break;
	 } 
}
