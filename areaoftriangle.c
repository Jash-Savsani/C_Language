#include<stdio.h>
void main()
{
    float h,b,area;
    printf("Enter a Height:");
    scanf("%f",&h);
    printf("Enter a Base:");
    scanf("%f",&b);
    area=(h*b)/2;
    printf("Area of Triangle=%f",area);
}